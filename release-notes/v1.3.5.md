更新类型：Bug 修复

### 修复

- 修复自动检查更新只会对同一版本弹出一次提示、后续重启后需要手动点“检查更新”才能再次得知有新版本的问题。
- 将自动更新提示的去重范围收敛到单次运行内，既避免一次启动中重复打扰，也保留下次启动时的自动提醒。

### 验证

- 已补充自动更新提示在单次运行内去重、但重启后会重新提示同一版本的 Qt 回归测试。
- 已补充手动检查同一版本时仍可重复弹出更新提示的回归测试。
